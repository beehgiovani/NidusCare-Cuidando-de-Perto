<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="32dp">

    <ImageView
        android:id="@+id/imageViewOnboarding"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:contentDescription="Imagem da introdução"
        tools:src="@drawable/ic_logo" />

    <TextView
        android:id="@+id/textViewTitle"
        style="@style/niduscare.Text.Headline"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:gravity="center"
        tools:text="Bem-vindo ao NidusCare!" />

    <TextView
        android:id="@+id/textViewDescription"
        style="@style/niduscare.Text.Body"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:gravity="center"
        android:lineSpacingMultiplier="1.2"
        tools:text="Seu assistente completo para o gerenciamento da saúde. Organize tudo em um só lugar." />

</LinearLayout>